/*
 * 网易云音乐 模块
 */
// import * as types from '../mutation-types'
const moduleNem = {
  namespaced: true,
  state: {},
  actions: {
    async init ({ state, dispatch, rootState }) {}
  }
}

export default moduleNem
